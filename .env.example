DATABASE_URL="postgresql://user:password@localhost:5432/second_chance_nc?schema=public"

# Stopgap password gate (src/middleware.ts) until real staff auth ships.
# Leave unset locally to skip the prompt; set both in production.
ADMIN_USERNAME="secondchance"
ADMIN_PASSWORD=""
